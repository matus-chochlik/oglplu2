//  File test/oglplus/enums/path_metric_query_nv.cpp
//
//  Automatically generated file, DO NOT modify manually.
//  Edit the enumeration source files in 'source/enums/oglplus'
//  or the 'source/enums/make_enum.py' script instead.
//
//  Copyright Matus Chochlik.
//  Distributed under the Boost Software License, Version 1.0.
//  See accompanying file LICENSE_1_0.txt or copy at
//   http://www.boost.org/LICENSE_1_0.txt
//
#define BOOST_TEST_DYN_LINK
#define BOOST_TEST_MODULE OGLPLUS_path_metric_query_nv

#include <boost/test/unit_test.hpp>
#include "common.hpp"
#include <cstring>

BOOST_AUTO_TEST_SUITE(enum_path_metric_query_nv_tests)

BOOST_AUTO_TEST_CASE(enum_path_metric_query_nv_values)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	path_metric_query_nv x;
	(void)x;

#ifdef GL_FONT_ASCENDER_BIT_NV
	x = ev.font_ascender_bit_nv;
	BOOST_CHECK(x == ev.font_ascender_bit_nv);
# ifdef GL_FONT_DESCENDER_BIT_NV
	BOOST_CHECK(x != ev.font_descender_bit_nv);
# endif
# ifdef GL_FONT_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.font_has_kerning_bit_nv);
# endif
# ifdef GL_FONT_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_width_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	BOOST_CHECK(x != ev.font_underline_position_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_DESCENDER_BIT_NV
	x = ev.font_descender_bit_nv;
	BOOST_CHECK(x == ev.font_descender_bit_nv);
# ifdef GL_FONT_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.font_has_kerning_bit_nv);
# endif
# ifdef GL_FONT_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_width_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	BOOST_CHECK(x != ev.font_underline_position_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_HAS_KERNING_BIT_NV
	x = ev.font_has_kerning_bit_nv;
	BOOST_CHECK(x == ev.font_has_kerning_bit_nv);
# ifdef GL_FONT_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_width_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	BOOST_CHECK(x != ev.font_underline_position_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_HEIGHT_BIT_NV
	x = ev.font_height_bit_nv;
	BOOST_CHECK(x == ev.font_height_bit_nv);
# ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_height_bit_nv);
# endif
# ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_width_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	BOOST_CHECK(x != ev.font_underline_position_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	x = ev.font_max_advance_height_bit_nv;
	BOOST_CHECK(x == ev.font_max_advance_height_bit_nv);
# ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.font_max_advance_width_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	BOOST_CHECK(x != ev.font_underline_position_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	x = ev.font_max_advance_width_bit_nv;
	BOOST_CHECK(x == ev.font_max_advance_width_bit_nv);
# ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	BOOST_CHECK(x != ev.font_underline_position_bit_nv);
# endif
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	x = ev.font_underline_position_bit_nv;
	BOOST_CHECK(x == ev.font_underline_position_bit_nv);
# ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	BOOST_CHECK(x != ev.font_underline_thickness_bit_nv);
# endif
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	x = ev.font_underline_thickness_bit_nv;
	BOOST_CHECK(x == ev.font_underline_thickness_bit_nv);
# ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	BOOST_CHECK(x != ev.font_units_per_em_bit_nv);
# endif
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	x = ev.font_units_per_em_bit_nv;
	BOOST_CHECK(x == ev.font_units_per_em_bit_nv);
# ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	x = ev.font_x_max_bounds_bit_nv;
	BOOST_CHECK(x == ev.font_x_max_bounds_bit_nv);
# ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_x_min_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	x = ev.font_x_min_bounds_bit_nv;
	BOOST_CHECK(x == ev.font_x_min_bounds_bit_nv);
# ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_max_bounds_bit_nv);
# endif
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	x = ev.font_y_max_bounds_bit_nv;
	BOOST_CHECK(x == ev.font_y_max_bounds_bit_nv);
# ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	BOOST_CHECK(x != ev.font_y_min_bounds_bit_nv);
# endif
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	x = ev.font_y_min_bounds_bit_nv;
	BOOST_CHECK(x == ev.font_y_min_bounds_bit_nv);
# ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	BOOST_CHECK(x != ev.glyph_has_kerning_bit_nv);
# endif
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	x = ev.glyph_has_kerning_bit_nv;
	BOOST_CHECK(x == ev.glyph_has_kerning_bit_nv);
# ifdef GL_GLYPH_HEIGHT_BIT_NV
	BOOST_CHECK(x != ev.glyph_height_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_HEIGHT_BIT_NV
	x = ev.glyph_height_bit_nv;
	BOOST_CHECK(x == ev.glyph_height_bit_nv);
# ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	x = ev.glyph_horizontal_bearing_advance_bit_nv;
	BOOST_CHECK(x == ev.glyph_horizontal_bearing_advance_bit_nv);
# ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	x = ev.glyph_horizontal_bearing_x_bit_nv;
	BOOST_CHECK(x == ev.glyph_horizontal_bearing_x_bit_nv);
# ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_horizontal_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	x = ev.glyph_horizontal_bearing_y_bit_nv;
	BOOST_CHECK(x == ev.glyph_horizontal_bearing_y_bit_nv);
# ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_advance_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	x = ev.glyph_vertical_bearing_advance_bit_nv;
	BOOST_CHECK(x == ev.glyph_vertical_bearing_advance_bit_nv);
# ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_x_bit_nv);
# endif
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	x = ev.glyph_vertical_bearing_x_bit_nv;
	BOOST_CHECK(x == ev.glyph_vertical_bearing_x_bit_nv);
# ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	BOOST_CHECK(x != ev.glyph_vertical_bearing_y_bit_nv);
# endif
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	x = ev.glyph_vertical_bearing_y_bit_nv;
	BOOST_CHECK(x == ev.glyph_vertical_bearing_y_bit_nv);
# ifdef GL_GLYPH_WIDTH_BIT_NV
	BOOST_CHECK(x != ev.glyph_width_bit_nv);
# endif
#endif

#ifdef GL_GLYPH_WIDTH_BIT_NV
	x = ev.glyph_width_bit_nv;
	BOOST_CHECK(x == ev.glyph_width_bit_nv);
#endif
}

BOOST_AUTO_TEST_CASE(enum_path_metric_query_nv_names)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	path_metric_query_nv x;
	(void)x;

#ifdef GL_FONT_ASCENDER_BIT_NV
	x = ev.font_ascender_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_ASCENDER_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_DESCENDER_BIT_NV
	x = ev.font_descender_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_DESCENDER_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_HAS_KERNING_BIT_NV
	x = ev.font_has_kerning_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_HAS_KERNING_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_HEIGHT_BIT_NV
	x = ev.font_height_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_HEIGHT_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	x = ev.font_max_advance_height_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_MAX_ADVANCE_HEIGHT_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	x = ev.font_max_advance_width_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_MAX_ADVANCE_WIDTH_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	x = ev.font_underline_position_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_UNDERLINE_POSITION_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	x = ev.font_underline_thickness_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_UNDERLINE_THICKNESS_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	x = ev.font_units_per_em_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_UNITS_PER_EM_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	x = ev.font_x_max_bounds_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_X_MAX_BOUNDS_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	x = ev.font_x_min_bounds_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_X_MIN_BOUNDS_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	x = ev.font_y_max_bounds_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_Y_MAX_BOUNDS_BIT_NV"
	) == 0);
#endif

#ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	x = ev.font_y_min_bounds_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FONT_Y_MIN_BOUNDS_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	x = ev.glyph_has_kerning_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_HAS_KERNING_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_HEIGHT_BIT_NV
	x = ev.glyph_height_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_HEIGHT_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	x = ev.glyph_horizontal_bearing_advance_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	x = ev.glyph_horizontal_bearing_x_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_HORIZONTAL_BEARING_X_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	x = ev.glyph_horizontal_bearing_y_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_HORIZONTAL_BEARING_Y_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	x = ev.glyph_vertical_bearing_advance_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	x = ev.glyph_vertical_bearing_x_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_VERTICAL_BEARING_X_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	x = ev.glyph_vertical_bearing_y_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_VERTICAL_BEARING_Y_BIT_NV"
	) == 0);
#endif

#ifdef GL_GLYPH_WIDTH_BIT_NV
	x = ev.glyph_width_bit_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"GLYPH_WIDTH_BIT_NV"
	) == 0);
#endif
}

BOOST_AUTO_TEST_CASE(enum_path_metric_query_nv_range)
{
	using namespace oglplus;
	auto count = enum_value_range<path_metric_query_nv>().size();

#ifdef GL_FONT_ASCENDER_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_ASCENDER_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_DESCENDER_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_DESCENDER_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_HAS_KERNING_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_HAS_KERNING_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_HEIGHT_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_HEIGHT_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_UNDERLINE_POSITION_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_UNDERLINE_THICKNESS_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_UNITS_PER_EM_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_UNITS_PER_EM_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_X_MAX_BOUNDS_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_X_MIN_BOUNDS_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_Y_MAX_BOUNDS_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_FONT_Y_MIN_BOUNDS_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_HAS_KERNING_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_HAS_KERNING_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_HEIGHT_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_HEIGHT_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_VERTICAL_BEARING_X_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV)
	) != r.end());
}
#endif

#ifdef GL_GLYPH_WIDTH_BIT_NV
{
	--count;
	auto r = enum_value_range<path_metric_query_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		path_metric_query_nv(GL_GLYPH_WIDTH_BIT_NV)
	) != r.end());
}
#endif
	BOOST_CHECK_EQUAL(count, 0);
}

BOOST_AUTO_TEST_CASE(enum_path_metric_query_nv_any)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	path_metric_query_nv x, y;
	(void)x;
	(void)y;
	any_enum_value a;
	(void)a;

#ifdef GL_FONT_ASCENDER_BIT_NV
	x = ev.font_ascender_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_ascender_bit_nv);
#endif

#ifdef GL_FONT_DESCENDER_BIT_NV
	x = ev.font_descender_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_descender_bit_nv);
#endif

#ifdef GL_FONT_HAS_KERNING_BIT_NV
	x = ev.font_has_kerning_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_has_kerning_bit_nv);
#endif

#ifdef GL_FONT_HEIGHT_BIT_NV
	x = ev.font_height_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_height_bit_nv);
#endif

#ifdef GL_FONT_MAX_ADVANCE_HEIGHT_BIT_NV
	x = ev.font_max_advance_height_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_max_advance_height_bit_nv);
#endif

#ifdef GL_FONT_MAX_ADVANCE_WIDTH_BIT_NV
	x = ev.font_max_advance_width_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_max_advance_width_bit_nv);
#endif

#ifdef GL_FONT_UNDERLINE_POSITION_BIT_NV
	x = ev.font_underline_position_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_underline_position_bit_nv);
#endif

#ifdef GL_FONT_UNDERLINE_THICKNESS_BIT_NV
	x = ev.font_underline_thickness_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_underline_thickness_bit_nv);
#endif

#ifdef GL_FONT_UNITS_PER_EM_BIT_NV
	x = ev.font_units_per_em_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_units_per_em_bit_nv);
#endif

#ifdef GL_FONT_X_MAX_BOUNDS_BIT_NV
	x = ev.font_x_max_bounds_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_x_max_bounds_bit_nv);
#endif

#ifdef GL_FONT_X_MIN_BOUNDS_BIT_NV
	x = ev.font_x_min_bounds_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_x_min_bounds_bit_nv);
#endif

#ifdef GL_FONT_Y_MAX_BOUNDS_BIT_NV
	x = ev.font_y_max_bounds_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_y_max_bounds_bit_nv);
#endif

#ifdef GL_FONT_Y_MIN_BOUNDS_BIT_NV
	x = ev.font_y_min_bounds_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.font_y_min_bounds_bit_nv);
#endif

#ifdef GL_GLYPH_HAS_KERNING_BIT_NV
	x = ev.glyph_has_kerning_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_has_kerning_bit_nv);
#endif

#ifdef GL_GLYPH_HEIGHT_BIT_NV
	x = ev.glyph_height_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_height_bit_nv);
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_ADVANCE_BIT_NV
	x = ev.glyph_horizontal_bearing_advance_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_horizontal_bearing_advance_bit_nv);
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_X_BIT_NV
	x = ev.glyph_horizontal_bearing_x_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_horizontal_bearing_x_bit_nv);
#endif

#ifdef GL_GLYPH_HORIZONTAL_BEARING_Y_BIT_NV
	x = ev.glyph_horizontal_bearing_y_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_horizontal_bearing_y_bit_nv);
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_ADVANCE_BIT_NV
	x = ev.glyph_vertical_bearing_advance_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_vertical_bearing_advance_bit_nv);
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_X_BIT_NV
	x = ev.glyph_vertical_bearing_x_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_vertical_bearing_x_bit_nv);
#endif

#ifdef GL_GLYPH_VERTICAL_BEARING_Y_BIT_NV
	x = ev.glyph_vertical_bearing_y_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_vertical_bearing_y_bit_nv);
#endif

#ifdef GL_GLYPH_WIDTH_BIT_NV
	x = ev.glyph_width_bit_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.glyph_width_bit_nv);
#endif
}

BOOST_AUTO_TEST_SUITE_END()
