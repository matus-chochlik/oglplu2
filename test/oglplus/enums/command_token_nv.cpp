//  File test/oglplus/enums/command_token_nv.cpp
//
//  Automatically generated file, DO NOT modify manually.
//  Edit the enumeration source files in 'source/enums/oglplus'
//  or the 'source/enums/make_enum.py' script instead.
//
//  Copyright Matus Chochlik.
//  Distributed under the Boost Software License, Version 1.0.
//  See accompanying file LICENSE_1_0.txt or copy at
//   http://www.boost.org/LICENSE_1_0.txt
//
#define BOOST_TEST_DYN_LINK
#define BOOST_TEST_MODULE OGLPLUS_command_token_nv

#include <boost/test/unit_test.hpp>
#include "common.hpp"
#include <cstring>

BOOST_AUTO_TEST_SUITE(enum_command_token_nv_tests)

BOOST_AUTO_TEST_CASE(enum_command_token_nv_values)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	command_token_nv x;
	(void)x;

#ifdef GL_ALPHA_REF_COMMAND_NV
	x = ev.alpha_ref_command_nv;
	BOOST_CHECK(x == ev.alpha_ref_command_nv);
# ifdef GL_ATTRIBUTE_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.attribute_address_command_nv);
# endif
# ifdef GL_BLEND_COLOR_COMMAND_NV
	BOOST_CHECK(x != ev.blend_color_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_instanced_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_strip_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_ATTRIBUTE_ADDRESS_COMMAND_NV
	x = ev.attribute_address_command_nv;
	BOOST_CHECK(x == ev.attribute_address_command_nv);
# ifdef GL_BLEND_COLOR_COMMAND_NV
	BOOST_CHECK(x != ev.blend_color_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_instanced_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_strip_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_BLEND_COLOR_COMMAND_NV
	x = ev.blend_color_command_nv;
	BOOST_CHECK(x == ev.blend_color_command_nv);
# ifdef GL_DRAW_ARRAYS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_instanced_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_strip_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_DRAW_ARRAYS_COMMAND_NV
	x = ev.draw_arrays_command_nv;
	BOOST_CHECK(x == ev.draw_arrays_command_nv);
# ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_instanced_command_nv);
# endif
# ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_strip_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	x = ev.draw_arrays_instanced_command_nv;
	BOOST_CHECK(x == ev.draw_arrays_instanced_command_nv);
# ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_arrays_strip_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	x = ev.draw_arrays_strip_command_nv;
	BOOST_CHECK(x == ev.draw_arrays_strip_command_nv);
# ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	x = ev.draw_elements_command_nv;
	BOOST_CHECK(x == ev.draw_elements_command_nv);
# ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_instanced_command_nv);
# endif
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	x = ev.draw_elements_instanced_command_nv;
	BOOST_CHECK(x == ev.draw_elements_instanced_command_nv);
# ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	BOOST_CHECK(x != ev.draw_elements_strip_command_nv);
# endif
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	x = ev.draw_elements_strip_command_nv;
	BOOST_CHECK(x == ev.draw_elements_strip_command_nv);
# ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.element_address_command_nv);
# endif
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	x = ev.element_address_command_nv;
	BOOST_CHECK(x == ev.element_address_command_nv);
# ifdef GL_FRONT_FACE_COMMAND_NV
	BOOST_CHECK(x != ev.front_face_command_nv);
# endif
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_FRONT_FACE_COMMAND_NV
	x = ev.front_face_command_nv;
	BOOST_CHECK(x == ev.front_face_command_nv);
# ifdef GL_LINE_WIDTH_COMMAND_NV
	BOOST_CHECK(x != ev.line_width_command_nv);
# endif
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_LINE_WIDTH_COMMAND_NV
	x = ev.line_width_command_nv;
	BOOST_CHECK(x == ev.line_width_command_nv);
# ifdef GL_NOP_COMMAND_NV
	BOOST_CHECK(x != ev.nop_command_nv);
# endif
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_NOP_COMMAND_NV
	x = ev.nop_command_nv;
	BOOST_CHECK(x == ev.nop_command_nv);
# ifdef GL_POLYGON_OFFSET_COMMAND_NV
	BOOST_CHECK(x != ev.polygon_offset_command_nv);
# endif
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_POLYGON_OFFSET_COMMAND_NV
	x = ev.polygon_offset_command_nv;
	BOOST_CHECK(x == ev.polygon_offset_command_nv);
# ifdef GL_SCISSOR_COMMAND_NV
	BOOST_CHECK(x != ev.scissor_command_nv);
# endif
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_SCISSOR_COMMAND_NV
	x = ev.scissor_command_nv;
	BOOST_CHECK(x == ev.scissor_command_nv);
# ifdef GL_STENCIL_REF_COMMAND_NV
	BOOST_CHECK(x != ev.stencil_ref_command_nv);
# endif
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_STENCIL_REF_COMMAND_NV
	x = ev.stencil_ref_command_nv;
	BOOST_CHECK(x == ev.stencil_ref_command_nv);
# ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	BOOST_CHECK(x != ev.terminate_sequence_command_nv);
# endif
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	x = ev.terminate_sequence_command_nv;
	BOOST_CHECK(x == ev.terminate_sequence_command_nv);
# ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	BOOST_CHECK(x != ev.uniform_address_command_nv);
# endif
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	x = ev.uniform_address_command_nv;
	BOOST_CHECK(x == ev.uniform_address_command_nv);
# ifdef GL_VIEWPORT_COMMAND_NV
	BOOST_CHECK(x != ev.viewport_command_nv);
# endif
#endif

#ifdef GL_VIEWPORT_COMMAND_NV
	x = ev.viewport_command_nv;
	BOOST_CHECK(x == ev.viewport_command_nv);
#endif
}

BOOST_AUTO_TEST_CASE(enum_command_token_nv_names)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	command_token_nv x;
	(void)x;

#ifdef GL_ALPHA_REF_COMMAND_NV
	x = ev.alpha_ref_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"ALPHA_REF_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_ATTRIBUTE_ADDRESS_COMMAND_NV
	x = ev.attribute_address_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"ATTRIBUTE_ADDRESS_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_BLEND_COLOR_COMMAND_NV
	x = ev.blend_color_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"BLEND_COLOR_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_DRAW_ARRAYS_COMMAND_NV
	x = ev.draw_arrays_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DRAW_ARRAYS_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	x = ev.draw_arrays_instanced_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DRAW_ARRAYS_INSTANCED_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	x = ev.draw_arrays_strip_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DRAW_ARRAYS_STRIP_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	x = ev.draw_elements_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DRAW_ELEMENTS_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	x = ev.draw_elements_instanced_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DRAW_ELEMENTS_INSTANCED_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	x = ev.draw_elements_strip_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DRAW_ELEMENTS_STRIP_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	x = ev.element_address_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"ELEMENT_ADDRESS_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_FRONT_FACE_COMMAND_NV
	x = ev.front_face_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FRONT_FACE_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_LINE_WIDTH_COMMAND_NV
	x = ev.line_width_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"LINE_WIDTH_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_NOP_COMMAND_NV
	x = ev.nop_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"NOP_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_POLYGON_OFFSET_COMMAND_NV
	x = ev.polygon_offset_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"POLYGON_OFFSET_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_SCISSOR_COMMAND_NV
	x = ev.scissor_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"SCISSOR_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_STENCIL_REF_COMMAND_NV
	x = ev.stencil_ref_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"STENCIL_REF_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	x = ev.terminate_sequence_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"TERMINATE_SEQUENCE_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	x = ev.uniform_address_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"UNIFORM_ADDRESS_COMMAND_NV"
	) == 0);
#endif

#ifdef GL_VIEWPORT_COMMAND_NV
	x = ev.viewport_command_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"VIEWPORT_COMMAND_NV"
	) == 0);
#endif
}

BOOST_AUTO_TEST_CASE(enum_command_token_nv_range)
{
	using namespace oglplus;
	auto count = enum_value_range<command_token_nv>().size();

#ifdef GL_ALPHA_REF_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_ALPHA_REF_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_ATTRIBUTE_ADDRESS_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_ATTRIBUTE_ADDRESS_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_BLEND_COLOR_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_BLEND_COLOR_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_DRAW_ARRAYS_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_DRAW_ARRAYS_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_DRAW_ARRAYS_STRIP_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_DRAW_ELEMENTS_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_DRAW_ELEMENTS_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_DRAW_ELEMENTS_STRIP_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_ELEMENT_ADDRESS_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_FRONT_FACE_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_FRONT_FACE_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_LINE_WIDTH_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_LINE_WIDTH_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_NOP_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_NOP_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_POLYGON_OFFSET_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_POLYGON_OFFSET_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_SCISSOR_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_SCISSOR_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_STENCIL_REF_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_STENCIL_REF_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_TERMINATE_SEQUENCE_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_UNIFORM_ADDRESS_COMMAND_NV)
	) != r.end());
}
#endif

#ifdef GL_VIEWPORT_COMMAND_NV
{
	--count;
	auto r = enum_value_range<command_token_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		command_token_nv(GL_VIEWPORT_COMMAND_NV)
	) != r.end());
}
#endif
	BOOST_CHECK_EQUAL(count, 0);
}

BOOST_AUTO_TEST_CASE(enum_command_token_nv_any)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	command_token_nv x, y;
	(void)x;
	(void)y;
	any_enum_value a;
	(void)a;

#ifdef GL_ALPHA_REF_COMMAND_NV
	x = ev.alpha_ref_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.alpha_ref_command_nv);
#endif

#ifdef GL_ATTRIBUTE_ADDRESS_COMMAND_NV
	x = ev.attribute_address_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.attribute_address_command_nv);
#endif

#ifdef GL_BLEND_COLOR_COMMAND_NV
	x = ev.blend_color_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.blend_color_command_nv);
#endif

#ifdef GL_DRAW_ARRAYS_COMMAND_NV
	x = ev.draw_arrays_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.draw_arrays_command_nv);
#endif

#ifdef GL_DRAW_ARRAYS_INSTANCED_COMMAND_NV
	x = ev.draw_arrays_instanced_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.draw_arrays_instanced_command_nv);
#endif

#ifdef GL_DRAW_ARRAYS_STRIP_COMMAND_NV
	x = ev.draw_arrays_strip_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.draw_arrays_strip_command_nv);
#endif

#ifdef GL_DRAW_ELEMENTS_COMMAND_NV
	x = ev.draw_elements_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.draw_elements_command_nv);
#endif

#ifdef GL_DRAW_ELEMENTS_INSTANCED_COMMAND_NV
	x = ev.draw_elements_instanced_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.draw_elements_instanced_command_nv);
#endif

#ifdef GL_DRAW_ELEMENTS_STRIP_COMMAND_NV
	x = ev.draw_elements_strip_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.draw_elements_strip_command_nv);
#endif

#ifdef GL_ELEMENT_ADDRESS_COMMAND_NV
	x = ev.element_address_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.element_address_command_nv);
#endif

#ifdef GL_FRONT_FACE_COMMAND_NV
	x = ev.front_face_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.front_face_command_nv);
#endif

#ifdef GL_LINE_WIDTH_COMMAND_NV
	x = ev.line_width_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.line_width_command_nv);
#endif

#ifdef GL_NOP_COMMAND_NV
	x = ev.nop_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.nop_command_nv);
#endif

#ifdef GL_POLYGON_OFFSET_COMMAND_NV
	x = ev.polygon_offset_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.polygon_offset_command_nv);
#endif

#ifdef GL_SCISSOR_COMMAND_NV
	x = ev.scissor_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.scissor_command_nv);
#endif

#ifdef GL_STENCIL_REF_COMMAND_NV
	x = ev.stencil_ref_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.stencil_ref_command_nv);
#endif

#ifdef GL_TERMINATE_SEQUENCE_COMMAND_NV
	x = ev.terminate_sequence_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.terminate_sequence_command_nv);
#endif

#ifdef GL_UNIFORM_ADDRESS_COMMAND_NV
	x = ev.uniform_address_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.uniform_address_command_nv);
#endif

#ifdef GL_VIEWPORT_COMMAND_NV
	x = ev.viewport_command_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.viewport_command_nv);
#endif
}

BOOST_AUTO_TEST_SUITE_END()
