//  File test/oglplus/enums/framebuffer_attachment.cpp
//
//  Automatically generated file, DO NOT modify manually.
//  Edit the enumeration source files in 'source/enums/oglplus'
//  or the 'source/enums/make_enum.py' script instead.
//
//  Copyright Matus Chochlik.
//  Distributed under the Boost Software License, Version 1.0.
//  See accompanying file LICENSE_1_0.txt or copy at
//   http://www.boost.org/LICENSE_1_0.txt
//
#define BOOST_TEST_DYN_LINK
#define BOOST_TEST_MODULE OGLPLUS_framebuffer_attachment

#include <boost/test/unit_test.hpp>
#include "common.hpp"
#include <cstring>

BOOST_AUTO_TEST_SUITE(enum_framebuffer_attachment)

BOOST_AUTO_TEST_CASE(enum_framebuffer_attachment_values)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	framebuffer_attachment x;
	(void)x;

#ifdef GL_COLOR_ATTACHMENT0
	x = ev.color_attachment0;
	BOOST_CHECK(x == ev.color_attachment0);
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT1
	x = ev.color_attachment1;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
	BOOST_CHECK(x == ev.color_attachment1);
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT10
	x = ev.color_attachment10;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
	BOOST_CHECK(x == ev.color_attachment10);
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT11
	x = ev.color_attachment11;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
	BOOST_CHECK(x == ev.color_attachment11);
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT12
	x = ev.color_attachment12;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
	BOOST_CHECK(x == ev.color_attachment12);
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT13
	x = ev.color_attachment13;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
	BOOST_CHECK(x == ev.color_attachment13);
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT14
	x = ev.color_attachment14;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
	BOOST_CHECK(x == ev.color_attachment14);
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT15
	x = ev.color_attachment15;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
	BOOST_CHECK(x == ev.color_attachment15);
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT2
	x = ev.color_attachment2;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
	BOOST_CHECK(x == ev.color_attachment2);
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT3
	x = ev.color_attachment3;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
	BOOST_CHECK(x == ev.color_attachment3);
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT4
	x = ev.color_attachment4;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
	BOOST_CHECK(x == ev.color_attachment4);
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT5
	x = ev.color_attachment5;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
	BOOST_CHECK(x == ev.color_attachment5);
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT6
	x = ev.color_attachment6;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
	BOOST_CHECK(x == ev.color_attachment6);
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT7
	x = ev.color_attachment7;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
	BOOST_CHECK(x == ev.color_attachment7);
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT8
	x = ev.color_attachment8;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
	BOOST_CHECK(x == ev.color_attachment8);
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_COLOR_ATTACHMENT9
	x = ev.color_attachment9;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
	BOOST_CHECK(x == ev.color_attachment9);
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_DEPTH_ATTACHMENT
	x = ev.depth_attachment;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
	BOOST_CHECK(x == ev.depth_attachment);
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_DEPTH_STENCIL_ATTACHMENT
	x = ev.depth_stencil_attachment;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
	BOOST_CHECK(x == ev.depth_stencil_attachment);
# ifdef GL_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.stencil_attachment);
# endif
#endif

#ifdef GL_STENCIL_ATTACHMENT
	x = ev.stencil_attachment;
# ifdef GL_COLOR_ATTACHMENT0
	BOOST_CHECK(x != ev.color_attachment0);
# endif
# ifdef GL_COLOR_ATTACHMENT1
	BOOST_CHECK(x != ev.color_attachment1);
# endif
# ifdef GL_COLOR_ATTACHMENT10
	BOOST_CHECK(x != ev.color_attachment10);
# endif
# ifdef GL_COLOR_ATTACHMENT11
	BOOST_CHECK(x != ev.color_attachment11);
# endif
# ifdef GL_COLOR_ATTACHMENT12
	BOOST_CHECK(x != ev.color_attachment12);
# endif
# ifdef GL_COLOR_ATTACHMENT13
	BOOST_CHECK(x != ev.color_attachment13);
# endif
# ifdef GL_COLOR_ATTACHMENT14
	BOOST_CHECK(x != ev.color_attachment14);
# endif
# ifdef GL_COLOR_ATTACHMENT15
	BOOST_CHECK(x != ev.color_attachment15);
# endif
# ifdef GL_COLOR_ATTACHMENT2
	BOOST_CHECK(x != ev.color_attachment2);
# endif
# ifdef GL_COLOR_ATTACHMENT3
	BOOST_CHECK(x != ev.color_attachment3);
# endif
# ifdef GL_COLOR_ATTACHMENT4
	BOOST_CHECK(x != ev.color_attachment4);
# endif
# ifdef GL_COLOR_ATTACHMENT5
	BOOST_CHECK(x != ev.color_attachment5);
# endif
# ifdef GL_COLOR_ATTACHMENT6
	BOOST_CHECK(x != ev.color_attachment6);
# endif
# ifdef GL_COLOR_ATTACHMENT7
	BOOST_CHECK(x != ev.color_attachment7);
# endif
# ifdef GL_COLOR_ATTACHMENT8
	BOOST_CHECK(x != ev.color_attachment8);
# endif
# ifdef GL_COLOR_ATTACHMENT9
	BOOST_CHECK(x != ev.color_attachment9);
# endif
# ifdef GL_DEPTH_ATTACHMENT
	BOOST_CHECK(x != ev.depth_attachment);
# endif
# ifdef GL_DEPTH_STENCIL_ATTACHMENT
	BOOST_CHECK(x != ev.depth_stencil_attachment);
# endif
	BOOST_CHECK(x == ev.stencil_attachment);
#endif
}

BOOST_AUTO_TEST_CASE(enum_framebuffer_attachment_names)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	framebuffer_attachment x;
	(void)x;

#ifdef GL_COLOR_ATTACHMENT0
	x = ev.color_attachment0;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT0"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT1
	x = ev.color_attachment1;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT1"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT10
	x = ev.color_attachment10;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT10"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT11
	x = ev.color_attachment11;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT11"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT12
	x = ev.color_attachment12;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT12"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT13
	x = ev.color_attachment13;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT13"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT14
	x = ev.color_attachment14;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT14"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT15
	x = ev.color_attachment15;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT15"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT2
	x = ev.color_attachment2;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT2"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT3
	x = ev.color_attachment3;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT3"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT4
	x = ev.color_attachment4;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT4"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT5
	x = ev.color_attachment5;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT5"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT6
	x = ev.color_attachment6;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT6"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT7
	x = ev.color_attachment7;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT7"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT8
	x = ev.color_attachment8;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT8"
	) == 0);
#endif

#ifdef GL_COLOR_ATTACHMENT9
	x = ev.color_attachment9;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ATTACHMENT9"
	) == 0);
#endif

#ifdef GL_DEPTH_ATTACHMENT
	x = ev.depth_attachment;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DEPTH_ATTACHMENT"
	) == 0);
#endif

#ifdef GL_DEPTH_STENCIL_ATTACHMENT
	x = ev.depth_stencil_attachment;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"DEPTH_STENCIL_ATTACHMENT"
	) == 0);
#endif

#ifdef GL_STENCIL_ATTACHMENT
	x = ev.stencil_attachment;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"STENCIL_ATTACHMENT"
	) == 0);
#endif
}

BOOST_AUTO_TEST_CASE(enum_framebuffer_attachment_range)
{
	using namespace oglplus;
	framebuffer_attachment x;
	(void)x;

#ifdef GL_COLOR_ATTACHMENT0
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT0
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT1
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT1
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT10
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT10
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT11
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT11
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT12
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT12
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT13
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT13
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT14
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT14
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT15
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT15
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT2
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT2
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT3
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT3
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT4
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT4
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT5
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT5
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT6
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT6
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT7
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT7
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT8
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT8
	) != r.end());
}
#endif

#ifdef GL_COLOR_ATTACHMENT9
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_COLOR_ATTACHMENT9
	) != r.end());
}
#endif

#ifdef GL_DEPTH_ATTACHMENT
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_DEPTH_ATTACHMENT
	) != r.end());
}
#endif

#ifdef GL_DEPTH_STENCIL_ATTACHMENT
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_DEPTH_STENCIL_ATTACHMENT
	) != r.end());
}
#endif

#ifdef GL_STENCIL_ATTACHMENT
{
	array_view<const GLenum> r = enum_value_range(x);
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		GL_STENCIL_ATTACHMENT
	) != r.end());
}
#endif
}

BOOST_AUTO_TEST_SUITE_END()
