//  File test/oglplus/enums/unified_array_address_nv.cpp
//
//  Automatically generated file, DO NOT modify manually.
//  Edit the enumeration source files in 'source/enums/oglplus'
//  or the 'source/enums/make_enum.py' script instead.
//
//  Copyright Matus Chochlik.
//  Distributed under the Boost Software License, Version 1.0.
//  See accompanying file LICENSE_1_0.txt or copy at
//   http://www.boost.org/LICENSE_1_0.txt
//
#define BOOST_TEST_DYN_LINK
#define BOOST_TEST_MODULE OGLPLUS_unified_array_address_nv

#include <boost/test/unit_test.hpp>
#include "common.hpp"
#include <cstring>

BOOST_AUTO_TEST_SUITE(enum_unified_array_address_nv_tests)

BOOST_AUTO_TEST_CASE(enum_unified_array_address_nv_values)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	unified_array_address_nv x;
	(void)x;

#ifdef GL_COLOR_ARRAY_ADDRESS_NV
	x = ev.color_array_address_nv;
	BOOST_CHECK(x == ev.color_array_address_nv);
# ifdef GL_EDGE_FLAG_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.edge_flag_array_address_nv);
# endif
# ifdef GL_ELEMENT_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.element_array_address_nv);
# endif
# ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.fog_coord_array_address_nv);
# endif
# ifdef GL_INDEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.index_array_address_nv);
# endif
# ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.normal_array_address_nv);
# endif
# ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.secondary_color_array_address_nv);
# endif
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_EDGE_FLAG_ARRAY_ADDRESS_NV
	x = ev.edge_flag_array_address_nv;
	BOOST_CHECK(x == ev.edge_flag_array_address_nv);
# ifdef GL_ELEMENT_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.element_array_address_nv);
# endif
# ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.fog_coord_array_address_nv);
# endif
# ifdef GL_INDEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.index_array_address_nv);
# endif
# ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.normal_array_address_nv);
# endif
# ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.secondary_color_array_address_nv);
# endif
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_ELEMENT_ARRAY_ADDRESS_NV
	x = ev.element_array_address_nv;
	BOOST_CHECK(x == ev.element_array_address_nv);
# ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.fog_coord_array_address_nv);
# endif
# ifdef GL_INDEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.index_array_address_nv);
# endif
# ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.normal_array_address_nv);
# endif
# ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.secondary_color_array_address_nv);
# endif
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
	x = ev.fog_coord_array_address_nv;
	BOOST_CHECK(x == ev.fog_coord_array_address_nv);
# ifdef GL_INDEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.index_array_address_nv);
# endif
# ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.normal_array_address_nv);
# endif
# ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.secondary_color_array_address_nv);
# endif
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_INDEX_ARRAY_ADDRESS_NV
	x = ev.index_array_address_nv;
	BOOST_CHECK(x == ev.index_array_address_nv);
# ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.normal_array_address_nv);
# endif
# ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.secondary_color_array_address_nv);
# endif
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	x = ev.normal_array_address_nv;
	BOOST_CHECK(x == ev.normal_array_address_nv);
# ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.secondary_color_array_address_nv);
# endif
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	x = ev.secondary_color_array_address_nv;
	BOOST_CHECK(x == ev.secondary_color_array_address_nv);
# ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.texture_coord_array_address_nv);
# endif
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	x = ev.texture_coord_array_address_nv;
	BOOST_CHECK(x == ev.texture_coord_array_address_nv);
# ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	BOOST_CHECK(x != ev.uniform_buffer_address_nv);
# endif
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	x = ev.uniform_buffer_address_nv;
	BOOST_CHECK(x == ev.uniform_buffer_address_nv);
# ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_array_address_nv);
# endif
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	x = ev.vertex_array_address_nv;
	BOOST_CHECK(x == ev.vertex_array_address_nv);
# ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	BOOST_CHECK(x != ev.vertex_attrib_array_address_nv);
# endif
#endif

#ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	x = ev.vertex_attrib_array_address_nv;
	BOOST_CHECK(x == ev.vertex_attrib_array_address_nv);
#endif
}

BOOST_AUTO_TEST_CASE(enum_unified_array_address_nv_names)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	unified_array_address_nv x;
	(void)x;

#ifdef GL_COLOR_ARRAY_ADDRESS_NV
	x = ev.color_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"COLOR_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_EDGE_FLAG_ARRAY_ADDRESS_NV
	x = ev.edge_flag_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"EDGE_FLAG_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_ELEMENT_ARRAY_ADDRESS_NV
	x = ev.element_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"ELEMENT_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
	x = ev.fog_coord_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"FOG_COORD_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_INDEX_ARRAY_ADDRESS_NV
	x = ev.index_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"INDEX_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	x = ev.normal_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"NORMAL_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	x = ev.secondary_color_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"SECONDARY_COLOR_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	x = ev.texture_coord_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"TEXTURE_COORD_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	x = ev.uniform_buffer_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"UNIFORM_BUFFER_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	x = ev.vertex_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"VERTEX_ARRAY_ADDRESS_NV"
	) == 0);
#endif

#ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	x = ev.vertex_attrib_array_address_nv;
	BOOST_ASSERT(enum_value_name(x).data() != nullptr);
	BOOST_CHECK(std::strcmp(
		enum_value_name(x).data(),
		"VERTEX_ATTRIB_ARRAY_ADDRESS_NV"
	) == 0);
#endif
}

BOOST_AUTO_TEST_CASE(enum_unified_array_address_nv_range)
{
	using namespace oglplus;
	auto count = enum_value_range<unified_array_address_nv>().size();

#ifdef GL_COLOR_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_COLOR_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_EDGE_FLAG_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_EDGE_FLAG_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_ELEMENT_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_ELEMENT_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_FOG_COORD_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_INDEX_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_INDEX_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_NORMAL_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_NORMAL_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_TEXTURE_COORD_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_UNIFORM_BUFFER_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_VERTEX_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_VERTEX_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif

#ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
{
	--count;
	auto r = enum_value_range<unified_array_address_nv>();
	BOOST_CHECK(std::find(
		r.begin(), r.end(),
		unified_array_address_nv(GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV)
	) != r.end());
}
#endif
	BOOST_CHECK_EQUAL(count, 0);
}

BOOST_AUTO_TEST_CASE(enum_unified_array_address_nv_any)
{
	using namespace oglplus;
	enum_values ev;
	(void)ev;
	unified_array_address_nv x, y;
	(void)x;
	(void)y;
	any_enum_value a;
	(void)a;

#ifdef GL_COLOR_ARRAY_ADDRESS_NV
	x = ev.color_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.color_array_address_nv);
#endif

#ifdef GL_EDGE_FLAG_ARRAY_ADDRESS_NV
	x = ev.edge_flag_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.edge_flag_array_address_nv);
#endif

#ifdef GL_ELEMENT_ARRAY_ADDRESS_NV
	x = ev.element_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.element_array_address_nv);
#endif

#ifdef GL_FOG_COORD_ARRAY_ADDRESS_NV
	x = ev.fog_coord_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.fog_coord_array_address_nv);
#endif

#ifdef GL_INDEX_ARRAY_ADDRESS_NV
	x = ev.index_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.index_array_address_nv);
#endif

#ifdef GL_NORMAL_ARRAY_ADDRESS_NV
	x = ev.normal_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.normal_array_address_nv);
#endif

#ifdef GL_SECONDARY_COLOR_ARRAY_ADDRESS_NV
	x = ev.secondary_color_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.secondary_color_array_address_nv);
#endif

#ifdef GL_TEXTURE_COORD_ARRAY_ADDRESS_NV
	x = ev.texture_coord_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.texture_coord_array_address_nv);
#endif

#ifdef GL_UNIFORM_BUFFER_ADDRESS_NV
	x = ev.uniform_buffer_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.uniform_buffer_address_nv);
#endif

#ifdef GL_VERTEX_ARRAY_ADDRESS_NV
	x = ev.vertex_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.vertex_array_address_nv);
#endif

#ifdef GL_VERTEX_ATTRIB_ARRAY_ADDRESS_NV
	x = ev.vertex_attrib_array_address_nv;
	a = x;
	y = a;
	BOOST_CHECK(same_enum_class(x, a));
	BOOST_CHECK(same_enum_class(a, y));
	BOOST_CHECK(same_enum_class(x, y));
	BOOST_CHECK(y == ev.vertex_attrib_array_address_nv);
#endif
}

BOOST_AUTO_TEST_SUITE_END()
